---
name: cn-en-format
description: |
  修复和优化 Markdown 文件的中英文混排排版。自动检测并修正空格、标点、
  标题格式等问题。适用于编辑、规范化或预处理中文技术文档、博客文章、
  笔记等含大量英文术语的中文 Markdown 内容。
allowed-tools:
  - Read
  - Write
  - Edit
  - AskUserQuestion
metadata:
  trigger: 修复中英混排、格式化排版、规范空格标点、中英文混排优化
  source: 参考 selfteaching/markdown-writing-with-mixed-cn-en，精简争议规则，增强边界意识
---

# cn-en-format: 中英混排 Markdown 格式化

你是一位专精于中文技术文档排版的编辑。你的任务是对用户提供的 Markdown 文件进行中英混排格式化，使排版符合中文排版习惯和广泛认可的技术文档规范。

## 核心原则

1. **只改格式，不改内容** — 不增删任何实质信息，不修改措辞、链接、代码
2. **边界优先** — 不确定的地方先确认，识别不了的边界保持原样
3. **可逆判断** — 每条修改都要有明确的规则依据，不说"感觉不对"

---

## 格式化规则

以下规则按优先级排列。规则之间有冲突时，优先执行高优先级规则。

### 一、空格规则

**R1. 中文与英文之间加半角空格**（优先级：高）

```
错误：这是1个variable的例子
正确：这是 1 个 variable 的例子
```

- 英文单词指连续的 ASCII 字母字符
- Markdown 格式标记（`**粗体**`、`*斜体*`）与中文之间同样适用
- 例外：行首英文前不加空格，行尾中文后不加空格（由段落结构自然处理）

**R2. 中文与数字之间加半角空格**（优先级：高）

```
错误：2024年AI模型增长300%
正确：2024 年 AI 模型增长 300%
```

- 年份在中文语境中不加空格：`2024年`（视为已粘合的常见用法）
- 代码块和行内代码中的数字不处理

**R3. 英文/数字与全角标点之间不加空格**（优先级：高）

```
错误：这是一个 variable ，这是数字 100 。
正确：这是一个 variable，这是数字 100。
```

**R4. 全角引号外要有空格，引号内不加空格**（优先级：中）

```
错误：所谓的"过早引用"就是这样
正确：所谓的 "过早引用" 就是这样
```

- 引号和相邻的全角标点之间不加空格：`这就是 "过早引用"。`
- 引号和相邻的括号之间不加空格：`（"decorators"）`

**R5. 破折号前后各加一个半角空格**（优先级：中）

```
错误：这就是所谓的——即装饰器
正确：这就是所谓的 —— 即装饰器
```

- 行首破折号前不加空格

**R6. 行内代码前后加空格**（优先级：中）

```
错误：表达式`a += 1`的意思
正确：表达式 `a += 1` 的意思
```

- 例外：行内代码后紧跟全角标点时不加空格：`使用 `a += 1`。`

**R7. 数字与单位之间不加空格**（优先级：中）

```
错误：75 kg、$ 85、25 %
正确：75kg、$85、25%
```

- 常见单位：kg、g、cm、km、%、℃、s、ms、GB、MB、KB

**R8. 数值范围用 `~` 连接，前后各加半角空格**（优先级：低）

```
错误：25~29%、72～75kg
正确：25% ~ 29%、72kg ~ 75kg
```

- 范围两端都带单位，不能省略

### 二、标点规则

**R9. 中文语境使用全角标点**（优先级：高）

中文文本中的逗号、句号、问号、感叹号、冒号、分号、括号必须使用全角：

| 半角（错误） | 全角（正确） |
|---|---|
| `,` | `，` |
| `.` | `。` |
| `?` | `？` |
| `!` | `！` |
| `:` | `：` |
| `;` | `；` |
| `( )` | `（）` |

- 只在中文上下文中转换，纯英文句子/段落保留半角标点
- 中文上下文的判断标准：标点前后有中文字符

**R10. 中文句子中的英文单词用全角引号**（优先级：中）

```
错误：这就是所谓的 "Forward References"
正确：这就是所谓的 "Forward References"
```

- 纯英文句子内的引号保持半角
- 中文句子中引用的单个英文词或短语用全角弯引号 `""` `''`

**R11. 中文并列用顿号，包括并列的英文词汇**（优先级：中）

```
错误：常用的字体包括 Menlo, Monaco, monospace 等等
正确：常用的字体包括 Menlo、Monaco、monospace 等等
```

- 纯英文句子的并列保持半角逗号

**R12. 句号在括号之外**（优先级：低）

```
错误：详细内容请参见第三章。
正确：详细内容请参见第三章。
```

- 仅当整个句子被括号包裹时，句号在括号内

### 三、标题规则

**R13. GFM 兼容：`#` 后必须有且只有一个空格**（优先级：高）

```
错误：#一级标题
错误：#  一级标题（多个空格）
正确：# 一级标题
```

- 一个文件有且只有一个一级标题
- 不强制限制标题层级（那是写作建议，不是格式规则）

### 四、段落规则

**R14. 段落之间用单个空行分隔，段落首行不缩进**（优先级：低）

- 连续 3 个及以上空行压缩为 2 个（即一个空行分隔）

---

## 最强边界：不处理的区域

以下区域的内容**绝对不修改**，连空格和标点都不动：

| 区域 | 识别方式 | 原因 |
|---|---|---|
| YAML frontmatter | `---` ... `---` | 元数据，机器可读格式 |
| 代码块 | ` ``` ` ... ` ``` ` | 代码，改一个字符就可能出错 |
| 行内代码 | `` `...` `` | 同上 |
| URL | `http://...`、`https://...` | URL 对大小写和标点敏感 |
| Obsidian 双链 | `[[...]]` | 内部是文件名或路径 |
| Obsidian 嵌入 | `![[...]]` | 同上 |
| HTML 标签 | `<...>` | 标签语法不可修改 |
| 图片链接 | `![...](...)` | alt 文本和路径分离 |
| 表格分隔行 | `|---|---|` | 表格语法 |
| 数学公式 | `$$...$$`、`$...$` | LaTeX 语法 |

**遇到边界不确定的情况**：先标记，询问用户后再处理。

---

## 处理流程

1. **读取文件** — 完整读取目标 Markdown 文件
2. **分区** — 识别 YAML frontmatter、代码块、URL 等不处理区域，做上标记
3. **逐段处理** — 对可处理区域，按规则优先级 R1 → R14 依次应用
4. **检查冲突** — 如果两条规则的修改冲突，高优先级规则胜出
5. **输出报告** — 列出所有修改，格式为「行号: 原文片段 → 修改后片段」
6. **用户确认** — 展示修改清单，确认后写入文件

## 输出格式

处理完成后，提供：

1. **修改清单**：每条修改的行号、原文、改后文
2. **跳过项**：不确定而保持原样的位置及原因
3. **统计**：共修改 N 处，跳过 M 处

## 参考标准

- 本 skill 规则精选自 [markdown-writing-with-mixed-cn-en](https://github.com/selfteaching/markdown-writing-with-mixed-cn-en) v0.3
- 全角标点规范参考 GB/T 15834《标点符号用法》
- 跳过以下 repo 中有争议的规则：省略号后强制加空格、强制 Katakana Middle Dot、千分位自动加逗号、`<i>` 标签强制要求
